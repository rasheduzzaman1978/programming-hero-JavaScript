const king = {
    name: 'Mufasa',
    kingdom: 'Pride Rock',
    age: 10,
    isAlive: true
};

const keys = Object.keys(king);
console.log(keys);

const values = Object.values(king);
console.log(values);

const entries = Object.entries(king);
console.log(entries);
